zuul:
  # 全部服务忽略敏感头（全部服务都可以设置cookie）
  sensitive-headers: 
  routes:
    myProduct:
      path: /myProduct/**
      serviceId: product
    user:
      path: /user/**
      serviceId: user
#    自定义路由简洁写法
#    product: /myProduct/**
#    排除
  ignored-patterns:
#    - /myProduct/product/listForOrder
#    - /product/product/listForOrder
#   排除简洁写法
    - /**/product/listForOrder
